name: VPN Server CI Demo

on:
  push:
    branches: [ "main" ]

jobs:
  vpn-job:
    runs-on: self-hosted

    steps:
      - name: Pull latest code on server
        run: |
          set -e
          cd /home/ubuntu/todo-react-supabase
          git fetch origin
          git pull origin main

      - name: Post-deploy confirmation
        run: |
          echo "Deployment completed on server 🚀"
          date
